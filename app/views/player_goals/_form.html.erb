<%= form_for(@player_goal) do |f| %>
  <% if @player_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_goal.errors.count, "error") %> prohibited this player_goal from being saved:</h2>

      <ul>
      <% @player_goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :tournament_id %><br>
    <%= f.number_field :tournament_id %>
  </div>
  <div class="field">
    <%= f.label :goals %><br>
    <%= f.number_field :goals %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
